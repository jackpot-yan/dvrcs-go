package tests

import (
	"testing"
)

func TestGRPCToolCall(t *testing.T) {
	// ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	// defer cancel()

	// TODO: 初始化 Executor + mock Tool
	// executor := NewExecutor()
	// toolCall := &ToolCall{
	// 	Name: "search",
	// 	Args: map[string]interface{}{
	// 		"query": "Tesla revenue",
	// 	},
	// }

	// resp, err := executor.Execute(ctx, toolCall)
	// if err != nil {
	// 	t.Fatalf("Executor failed: %v", err)
	// }

	// t.Logf("Tool response: %s", resp)
}
